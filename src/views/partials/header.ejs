<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog - Chia sẻ kiến thức lập trình</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        :root {
            /* Modern color palette */
            --primary: #3b82f6;
            --primary-dark: #2563eb;
            --secondary: #64748b;
            --accent: #06b6d4;
            --success: #22c55e;
            --warning: #f59e0b;
            --danger: #ef4444;
            --dark: #0f172a;
            --light: #f8fafc;

            /* Neutral colors */
            --gray-50: #f8fafc;
            --gray-100: #f1f5f9;
            --gray-200: #e2e8f0;
            --gray-300: #cbd5e1;
            --gray-400: #94a3b8;
            --gray-500: #64748b;
            --gray-600: #475569;
            --gray-700: #334155;
            --gray-800: #1e293b;
            --gray-900: #0f172a;

            /* Gradients */
            --gradient-primary: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            --gradient-dark: linear-gradient(135deg, var(--gray-800) 0%, var(--gray-900) 100%);
            --gradient-card: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(6, 182, 212, 0.1) 100%);

            /* Shadows */
            --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            --shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
            --shadow-md: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
            --shadow-lg: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
            --shadow-hover: 0 20px 25px -5px rgb(0 0 0 / 0.15), 0 8px 10px -6px rgb(0 0 0 / 0.15);

            /* Border radius */
            --radius-sm: 0.375rem;
            --radius: 0.5rem;
            --radius-lg: 0.75rem;
            --radius-xl: 1rem;

            /* Transitions */
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            --transition-fast: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            --transition-slow: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }

        /* Global Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideUp {
            from {
                transform: translateY(20px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes slideIn {
            from {
                transform: translateX(100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        @keyframes scaleIn {
            from {
                transform: scale(0.95);
                opacity: 0;
            }
            to {
                transform: scale(1);
                opacity: 1;
            }
        }

        /* Apply animations to elements */
        body {
            font-family: 'Inter', sans-serif;
            color: var(--gray-800);
            background-color: var(--gray-50);
            line-height: 1.6;
            animation: fadeIn 0.5s ease-out;
        }

        .container {
            animation: slideUp 0.5s ease-out;
        }

        /* Modern Navbar */
        .navbar {
            background: rgba(255,255,255,0.95);
            box-shadow: var(--shadow-sm);
            margin-bottom: 1rem;
            padding: 0.5rem 1.5rem;
            transition: var(--transition);
        }
        .navbar .container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
        }
        .navbar-brand {
            font-size: 2rem;
            font-weight: 800;
            color: var(--primary) !important;
            letter-spacing: -1px;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.25rem 0;
        }
        .navbar-brand i {
            font-size: 2.2rem;
        }
        .navbar-toggler {
            border: none;
            background: none;
            font-size: 2rem;
            color: var(--primary);
            display: none;
        }
        .navbar-collapse {
            display: flex !important;
            flex-basis: auto;
            flex-grow: 1;
            align-items: center;
            justify-content: space-between;
        }
        .navbar-nav {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            flex-direction: row;
        }
        .nav-link {
            font-size: 1rem;
            font-weight: 600;
            color: var(--gray-700) !important;
            border-radius: var(--radius-lg);
            padding: 0.5rem 1.1rem !important;
            margin: 0 0.1rem;
        }
        .nav-link.active, .nav-link:hover {
            color: var(--primary) !important;
            background: var(--gradient-card);
        }
        .nav-link i {
            margin-right: 0.4rem;
        }
        .navbar-nav:last-child .nav-link {
            margin-left: 0.5rem;
        }
        .user-info {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-weight: 600;
            color: var(--primary-dark);
            background: var(--gray-100);
            border-radius: var(--radius);
            padding: 0.3rem 0.9rem;
        }
        .user-info i {
            font-size: 1.3rem;
        }
        /* Hamburger for mobile */
        @media (max-width: 991.98px) {
            .navbar .container {
                flex-wrap: nowrap;
            }
            .navbar-toggler {
                display: block;
            }
            .navbar-collapse {
                display: none !important;
                flex-direction: column;
                width: 100%;
                background: rgba(255,255,255,0.98);
                box-shadow: var(--shadow-md);
                border-radius: var(--radius-lg);
                position: absolute;
                top: 100%;
                left: 0;
                z-index: 2000;
                padding: 1rem 0.5rem;
                margin-top: 0.5rem;
            }
            .navbar-collapse.show {
                display: flex !important;
            }
            .navbar-nav {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.2rem;
            }
            .navbar-nav:last-child {
                margin-top: 1rem;
                border-top: 1px solid var(--gray-200);
                padding-top: 0.5rem;
            }
            .user-info {
                width: 100%;
                justify-content: flex-start;
            }
        }
        /* Dropdown menu fix for mobile */
        @media (max-width: 991.98px) {
            .dropdown-menu {
                position: static !important;
                float: none;
                box-shadow: none;
                margin-top: 0.2rem;
            }
        }

        /* Modern Cards */
        .card {
            border: none;
            background: white;
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow);
            transition: var(--transition);
            overflow: hidden;
            position: relative;
            animation: scaleIn 0.5s ease-out;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-lg);
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--gradient-primary);
            opacity: 0;
            transition: var(--transition);
        }

        .card:hover::before {
            opacity: 1;
        }

        .card-header {
            background: white;
            border-bottom: 1px solid var(--gray-200);
            padding: 1.25rem;
            font-weight: 600;
            color: var(--gray-800);
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .card-header i {
            font-size: 1.25rem;
            color: var(--primary);
            background: var(--gradient-card);
            padding: 0.5rem;
            border-radius: var(--radius);
            transition: var(--transition);
        }

        .card:hover .card-header i {
            transform: scale(1.1);
        }

        .card-body {
            padding: 1.5rem;
        }

        /* Modern Buttons */
        .btn {
            font-weight: 500;
            padding: 0.625rem 1.25rem;
            border-radius: var(--radius);
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            position: relative;
            overflow: hidden;
        }

        .btn:hover {
            transform: translateY(-2px);
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0));
            transition: var(--transition);
        }

        .btn:hover::before {
            transform: translateY(-100%);
        }

        .btn-sm {
            padding: 0.5rem 1rem;
            font-size: 0.875rem;
        }

        .btn-primary {
            background: var(--gradient-primary);
            border: none;
            color: white;
        }

        .btn-gradient {
            background: var(--gradient-primary);
            border: none;
            color: white;
            box-shadow: var(--shadow);
        }

        /* Modern Badges */
        .badge {
            font-weight: 500;
            padding: 0.4em 0.8em;
            border-radius: var(--radius);
            font-size: 0.75rem;
            letter-spacing: 0.025em;
            transition: var(--transition);
        }

        .badge:hover {
            transform: scale(1.05);
        }

        .badge.bg-primary {
            background: var(--gradient-primary) !important;
        }

        /* Flash Messages */
        .flash-messages {
            position: fixed;
            top: 1rem;
            right: 1rem;
            z-index: 1050;
            max-width: 24rem;
        }

        .alert {
            border: none;
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            padding: 1rem 1.25rem;
            margin-bottom: 0.75rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            animation: slideIn 0.3s ease-out;
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
        }

        .alert i {
            font-size: 1.25rem;
            transition: var(--transition);
        }

        .alert:hover i {
            transform: scale(1.1);
        }

        /* Dropdown animations */
        .dropdown-menu {
            animation: scaleIn 0.2s ease-out;
            transform-origin: top right;
        }

        .dropdown-item {
            transition: var(--transition-fast);
        }

        .dropdown-item:hover {
            transform: translateX(5px);
        }

        /* Form elements */
        input, textarea, select {
            transition: var(--transition);
        }

        input:focus, textarea:focus, select:focus {
            transform: translateY(-2px);
        }

        /* Links */
        a {
            transition: var(--transition);
        }

        a:hover {
            transform: translateY(-1px);
        }
    </style>
</head>

<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg">
        <div class="container position-relative">
            <a class="navbar-brand" href="/">
                <i class="fas fa-code"></i>
                Tech Blog
            </a>
            <button class="navbar-toggler" type="button" aria-label="Toggle navigation" onclick="document.getElementById('navbarNav').classList.toggle('show')">
                <i class="fas fa-bars"></i>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/">
                            <i class="fas fa-home"></i>Trang chủ
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="categoriesDropdown" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-folder"></i>Chuyên mục
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow-lg">
                            <% if (locals.categories && categories.length > 0) { %>
                                <% categories.forEach(category => { %>
                                    <li>
                                        <a class="dropdown-item d-flex align-items-center gap-2 py-2" href="/posts?category=<%= category.id %>">
                                            <i class="fas fa-chevron-right text-primary"></i>
                                            <%= category.name %>
                                        </a>
                                    </li>
                                <% }); %>
                                <% if (locals.user && user.role === 'ADMIN') { %>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item d-flex align-items-center gap-2 py-2" href="/categories/new">
                                            <i class="fas fa-plus text-success"></i>Thêm chuyên mục
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item d-flex align-items-center gap-2 py-2" href="/categories/manage">
                                            <i class="fas fa-cog text-primary"></i>Quản lý chuyên mục
                                        </a>
                                    </li>
                                <% } %>
                            <% } else { %>
                                <li><a class="dropdown-item py-2">Chưa có chuyên mục</a></li>
                            <% } %>
                        </ul>
                    </li>
                    <% if (locals.user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/posts/new">
                                <i class="fas fa-plus"></i>Viết bài
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/my-posts">
                                <i class="fas fa-file-alt"></i>Bài viết của tôi
                            </a>
                        </li>
                    <% } %>
                </ul>
                <ul class="navbar-nav">
                    <% if (locals.user) { %>
                        <li class="nav-item">
                            <span class="user-info">
                                <i class="fas fa-user-circle"></i>
                                <%= user.name || user.email %>
                            </span>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/logout">
                                <i class="fas fa-sign-out-alt"></i>Đăng xuất
                            </a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">
                                <i class="fas fa-sign-in-alt"></i>Đăng nhập
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">
                                <i class="fas fa-user-plus"></i>Đăng ký
                            </a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
    <div class="flash-messages">
        <% if (locals.success_msg && success_msg.length> 0) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle"></i>
                <%= success_msg %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            <% } %>
                <% if (locals.error_msg && error_msg.length> 0) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="fas fa-exclamation-circle"></i>
                        <%= error_msg %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                    <% } %>
    </div>

    <!-- Main Content -->
    <div class="container">